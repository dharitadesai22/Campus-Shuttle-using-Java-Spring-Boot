package ShuttleProject.Shuttle.DataBases;

public enum UserStatus {
     IDLE,     // Indicates the user is idle
     WAITING,  // Indicates the user is waiting for the shuttle
     PICKEDUP; // Indicates the user has been picked up by the shuttle
}
